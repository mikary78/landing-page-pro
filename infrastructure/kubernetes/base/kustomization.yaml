# ============================================
# Kustomize Base Configuration
# ============================================
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: landing-page-pro

resources:
  - namespace.yaml
  - project-service.yaml
  - course-service.yaml
  - ai-service.yaml

commonLabels:
  app.kubernetes.io/part-of: landing-page-pro
  app.kubernetes.io/managed-by: kustomize
