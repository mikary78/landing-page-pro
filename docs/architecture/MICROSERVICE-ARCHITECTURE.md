# 마이크로서비스 아키텍처 설계

## 📋 개요

Landing Page Pro 프로젝트를 마이크로서비스 아키텍처로 전환하기 위한 설계 문서입니다.

## 🏗️ 현재 아키텍처 (Monolithic)

```
┌─────────────────────────────────────────────────────────────────┐
│                        Frontend (React SPA)                      │
└─────────────────────────────────────────────────────────────────┘
                                │
                                ▼
┌─────────────────────────────────────────────────────────────────┐
│                    Azure Functions (Monolithic)                  │
│  ┌─────────────┐ ┌─────────────┐ ┌─────────────┐ ┌────────────┐ │
│  │   Project   │ │   Course    │ │     AI      │ │  Template  │ │
│  │  Functions  │ │  Functions  │ │  Functions  │ │  Functions │ │
│  └─────────────┘ └─────────────┘ └─────────────┘ └────────────┘ │
└─────────────────────────────────────────────────────────────────┘
                                │
                                ▼
┌─────────────────────────────────────────────────────────────────┐
│                      PostgreSQL (Single DB)                      │
└─────────────────────────────────────────────────────────────────┘
```

## 🎯 목표 아키텍처 (Microservices)

```
┌─────────────────────────────────────────────────────────────────┐
│                        Frontend (React SPA)                      │
└─────────────────────────────────────────────────────────────────┘
                                │
                                ▼
┌─────────────────────────────────────────────────────────────────┐
│                    API Gateway (API Management)                  │
└─────────────────────────────────────────────────────────────────┘
          │              │              │              │
          ▼              ▼              ▼              ▼
┌──────────────┐ ┌──────────────┐ ┌──────────────┐ ┌──────────────┐
│   Project    │ │    Course    │ │      AI      │ │   Template   │
│   Service    │ │   Service    │ │   Service    │ │   Service    │
│  (Functions) │ │  (Functions) │ │  (Functions) │ │  (Functions) │
└──────────────┘ └──────────────┘ └──────────────┘ └──────────────┘
          │              │              │              │
          ▼              ▼              ▼              ▼
┌─────────────────────────────────────────────────────────────────┐
│                    Azure Service Bus (Event Bus)                 │
└─────────────────────────────────────────────────────────────────┘
          │              │              │              │
          ▼              ▼              ▼              ▼
┌──────────────┐ ┌──────────────┐ ┌──────────────┐ ┌──────────────┐
│   Project    │ │    Course    │ │      AI      │ │   Template   │
│     DB       │ │      DB      │ │  Cache/Queue │ │      DB      │
│ (PostgreSQL) │ │ (PostgreSQL) │ │   (Redis)    │ │ (PostgreSQL) │
└──────────────┘ └──────────────┘ └──────────────┘ └──────────────┘
```

## 📦 서비스 분리 계획

### 1. Project Service
프로젝트 관리 관련 기능

| 함수 | 설명 | HTTP Method |
|------|------|-------------|
| createProject | 프로젝트 생성 | POST |
| getProjects | 프로젝트 목록 | GET |
| getProjectDetail | 프로젝트 상세 | GET |
| updateProject | 프로젝트 수정 | PUT |
| deleteProject | 프로젝트 삭제 | DELETE |
| getProjectStages | 프로젝트 단계 | GET |
| updateProjectStage | 단계 업데이트 | PUT |

**데이터베이스 테이블:**
- projects
- project_stages
- project_documents

### 2. Course Service
강의/커리큘럼 관련 기능

| 함수 | 설명 | HTTP Method |
|------|------|-------------|
| createCourse | 강의 생성 | POST |
| getCourses | 강의 목록 | GET |
| getCourse | 강의 상세 | GET |
| getCoursePublic | 공개 강의 | GET |
| deleteCourse | 강의 삭제 | DELETE |
| createModule | 모듈 생성 | POST |
| updateModule | 모듈 수정 | PUT |
| getModulesWithLessons | 모듈+레슨 | GET |
| createLesson | 레슨 생성 | POST |
| updateLesson | 레슨 수정 | PUT |
| getLessonDetail | 레슨 상세 | GET |
| createLessonProject | 레슨 프로젝트 | POST |

**데이터베이스 테이블:**
- courses
- modules
- lessons
- lesson_projects

### 3. AI Service
AI 관련 기능 (Gemini, OpenAI, Anthropic)

| 함수 | 설명 | HTTP Method |
|------|------|-------------|
| generateCurriculum | 커리큘럼 생성 | POST |
| processDocument | 문서 처리 | POST |

**특징:**
- Stateless 서비스
- 외부 AI API 호출
- Redis 캐싱 사용 (응답 캐싱)
- Queue 기반 비동기 처리

### 4. Template Service
템플릿 관리 기능

| 함수 | 설명 | HTTP Method |
|------|------|-------------|
| getTemplates | 템플릿 목록 | GET |
| saveTemplate | 템플릿 저장 | POST |

**데이터베이스 테이블:**
- templates

### 5. Feedback Service
피드백 수집 기능

| 함수 | 설명 | HTTP Method |
|------|------|-------------|
| feedbackApi | 피드백 처리 | POST |

### 6. Stats Service
통계/분석 기능

| 함수 | 설명 | HTTP Method |
|------|------|-------------|
| getStats | 통계 조회 | GET |

### 7. Gateway Service (API Management)
API 라우팅, 인증, Rate Limiting

## 🔄 이벤트 기반 통신

### Azure Service Bus Topics

| Topic | 설명 | Publishers | Subscribers |
|-------|------|------------|-------------|
| project-events | 프로젝트 이벤트 | Project Service | Course Service, Stats Service |
| course-events | 강의 이벤트 | Course Service | Stats Service |
| ai-events | AI 처리 이벤트 | AI Service | Project Service, Course Service |
| user-events | 사용자 이벤트 | Auth Service | All Services |

### 이벤트 종류

```typescript
// Project Events
interface ProjectCreatedEvent {
  type: 'PROJECT_CREATED';
  projectId: string;
  userId: string;
  timestamp: string;
}

interface ProjectUpdatedEvent {
  type: 'PROJECT_UPDATED';
  projectId: string;
  changes: Record<string, any>;
  timestamp: string;
}

// Course Events
interface CourseCreatedEvent {
  type: 'COURSE_CREATED';
  courseId: string;
  projectId: string;
  userId: string;
  timestamp: string;
}

// AI Events
interface CurriculumGeneratedEvent {
  type: 'CURRICULUM_GENERATED';
  courseId: string;
  modulesCount: number;
  lessonsCount: number;
  timestamp: string;
}
```

## 📁 프로젝트 구조

```
landing-page-pro/
├── services/
│   ├── project-service/
│   │   ├── src/
│   │   │   ├── functions/
│   │   │   ├── models/
│   │   │   ├── events/
│   │   │   └── lib/
│   │   ├── Dockerfile
│   │   ├── host.json
│   │   └── package.json
│   │
│   ├── course-service/
│   │   ├── src/
│   │   │   ├── functions/
│   │   │   ├── models/
│   │   │   ├── events/
│   │   │   └── lib/
│   │   ├── Dockerfile
│   │   ├── host.json
│   │   └── package.json
│   │
│   ├── ai-service/
│   │   ├── src/
│   │   │   ├── functions/
│   │   │   ├── providers/
│   │   │   ├── events/
│   │   │   └── lib/
│   │   ├── Dockerfile
│   │   ├── host.json
│   │   └── package.json
│   │
│   ├── template-service/
│   │   ├── src/
│   │   │   ├── functions/
│   │   │   ├── models/
│   │   │   └── lib/
│   │   ├── Dockerfile
│   │   ├── host.json
│   │   └── package.json
│   │
│   └── shared/
│       ├── events/           # 공통 이벤트 타입
│       ├── auth/             # 인증 미들웨어
│       ├── db/               # DB 연결 유틸
│       └── service-bus/      # Service Bus 클라이언트
│
├── infrastructure/
│   ├── kubernetes/
│   │   ├── base/
│   │   ├── overlays/
│   │   │   ├── development/
│   │   │   ├── staging/
│   │   │   └── production/
│   │   └── kustomization.yaml
│   │
│   └── terraform/
│       ├── modules/
│       │   ├── aks/
│       │   ├── service-bus/
│       │   ├── postgresql/
│       │   └── redis/
│       ├── environments/
│       │   ├── dev/
│       │   ├── staging/
│       │   └── prod/
│       └── main.tf
│
└── frontend/                 # React SPA (기존 src 폴더)
```

## 🚀 마이그레이션 단계

### Phase 1: 준비 (1-2주)
1. ✅ 공유 라이브러리 구조 생성
2. ✅ 이벤트 타입 정의
3. ✅ Service Bus 클라이언트 구현
4. ✅ 데이터베이스 스키마 분리 설계

### Phase 2: 서비스 분리 (2-4주)
1. Project Service 분리
2. Course Service 분리
3. AI Service 분리
4. Template Service 분리

### Phase 3: 인프라 구축 (2-3주)
1. Azure Service Bus 설정
2. Azure Kubernetes Service 클러스터 생성
3. Helm Charts 작성
4. CI/CD 파이프라인 업데이트

### Phase 4: 배포 및 전환 (1-2주)
1. Staging 환경 테스트
2. Blue-Green 배포
3. 트래픽 전환
4. 모니터링 및 안정화

## 📊 비용 예상

| 리소스 | 스펙 | 월 예상 비용 |
|--------|------|-------------|
| AKS Cluster | 3 nodes (Standard_D2s_v3) | ~$150 |
| Azure Service Bus | Standard tier | ~$10 |
| PostgreSQL | Basic tier x 4 | ~$100 |
| Redis Cache | Basic C0 | ~$15 |
| Container Registry | Basic | ~$5 |
| **Total** | | **~$280/월** |

*참고: 실제 비용은 사용량에 따라 다를 수 있습니다.*

## 🔒 보안 고려사항

1. **서비스 간 통신**: mTLS (상호 TLS) 사용
2. **API Gateway**: JWT 검증, Rate Limiting
3. **Service Bus**: SAS 토큰 또는 Managed Identity
4. **데이터베이스**: VNet 내부에서만 접근, 암호화
5. **Secrets**: Azure Key Vault 사용

## 📚 참고 자료

- [Azure Architecture Center - Microservices](https://learn.microsoft.com/azure/architecture/microservices/)
- [Azure Service Bus](https://learn.microsoft.com/azure/service-bus-messaging/)
- [Azure Kubernetes Service](https://learn.microsoft.com/azure/aks/)
- [12-Factor App](https://12factor.net/)
